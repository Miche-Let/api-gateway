eureka:
  client:
    enabled: false

spring:
  cloud:
    discovery:
      enabled: false

jwt:
  secret: test-secret-key-test-secret-key-test-secret-key

cors:
  allowed-origins:
    - http://localhost:3000